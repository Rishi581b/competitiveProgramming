#include <iostream>
#define ll unsigned long long int
using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        ll x;
        cin >> x;
        cout << "0 " << x << "\n";
    }
}